# Plugin Policy

No placeholder Unreal plugins are created for RecordShop. The project currently has no `Plugins/` directory, so it is intentionally left absent.

If a project-specific plugin is justified later, create it through Unreal's normal plugin workflow under `Plugins/<PluginName>/`.

